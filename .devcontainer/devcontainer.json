{
  "name": "Streamlit App Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12.8"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgeos-dev && pip install -r requirements.txt && git clone https://github.com/hmarichal93/cstrd_ipol.git && cd cstrd_ipol && pip install . && cd .. && git clone https://github.com/hmarichal93/uruDendro.git && cd uruDendro && pip install .",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "forwardPorts": [8501]
}
